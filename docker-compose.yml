version: "3.9"

services:
  portfolio:
    build:
      context: ./portfolio
      dockerfile: Dockerfile
    container_name: portfolio-app
    ports:
      - "127.0.0.1:4000:80"
    restart: unless-stopped